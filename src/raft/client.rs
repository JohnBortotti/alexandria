// use super::server::Server;

// pub struct Client {
//     server: Server
// }

/* todo:
 * Client struct is the database user API,
 * the Client struct will handle 
 * user requests and parse to Event::ClientRequest,
 * send the query to a peer and handle the rest of the pipeline
*/
// impl Client {
//     pub async fn new(server: Server) -> Self {
//         Client { server }
//     }
// }
